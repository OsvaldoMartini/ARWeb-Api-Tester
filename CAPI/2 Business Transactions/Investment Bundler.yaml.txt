openapi: "3.0.2"
security:
  - bearerAuth: []

info:
  title: "Investment Bundler API"
  version: "3.12.2"
  x-avq$categories:
    - capi

servers:
- url: "/api1/"
  description: "Development server"

paths:
  /doc-invst-bdls:
    get:
      tags:
      - API endpoints
      summary: Get Investment Bundler orders.
      description: |
        Get a collection of `Investment Bundler` orders.
        The result set can be restricted through filter parameter.
        Only `Investment Bundler` orders will be returned that can be accessed by the user (security applied).
        Paging is supported with parameters *limit* and *offset*.
    
      parameters:
      - $ref: '#/components/parameters/Filter'
      - $ref: '#/components/parameters/DocPool'
      - $ref: '#/components/parameters/PageLimit'
      - $ref: '#/components/parameters/PageOffset'
      - $ref: '#/components/parameters/HrefActive'
      - $ref: '#/components/parameters/IncludeInactive'
      - $ref: '#/components/parameters/Accept-Encoding'
      - $ref: '#/components/parameters/Accept-Language'
    
      responses:
        '200':
          description: Successful response
          headers:
            Content-Language:
              $ref: '#/components/headers/Content-Language'
            X-Has-Next-Page:
              $ref: '#/components/headers/X-Has-Next-Page'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/doc_invst_bdl'
        'default':
          $ref: '#/components/responses/Problem'
    post:
      tags:
      - API endpoints
      summary: Create a new Investment Bundler order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/doc_invst_bdl'
          application/xml:
            schema:
              $ref: '#/components/schemas/doc_invst_bdl'
      parameters:
      - $ref: '#/components/parameters/X-Request-ID'
      - $ref: '#/components/parameters/Accept-Encoding'
      responses:
        '201':
          description: Successfully created a new `Investment Bundler` order
          headers:
            Location:
              $ref: '#/components/headers/Location'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                    format: int64
                    example: 123456
                  bdeRecVersion:
                    type: integer
                    format: int64
                    example: 1618390720445656
        'default':
          $ref: '#/components/responses/Problem'

  /doc-invst-bdls/{doc_invst_bdl_id}:
    get:
      tags:
      - API endpoints
      summary: "Get a Investment Bundler order"
      description: |
        *Get a `Investment Bundler` order by ID*.
        Only `Investment Bundler` order will be returned that can be accessed by the user (security applied).
        If user does not have access to resource, not found error will be returned instead of unauthorized - to disallow attacks.
      parameters:
      - in: path
        name: doc_invst_bdl_id
        schema:
          type: string
        required: true
        description: ID of the `Investment Bundler` order to get
      - $ref: '#/components/parameters/HrefActive'
      - $ref: '#/components/parameters/IncludeInactive'
      - $ref: '#/components/parameters/Accept-Encoding'
      - $ref: '#/components/parameters/If-None-Match'
      - $ref: '#/components/parameters/Accept-Language'
      responses:
        '200':
          description: Successful response
          headers:
            Content-Language:
              $ref: '#/components/headers/Content-Language'
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/doc_invst_bdl'
        '304':
          description: Not modified
        'default':
          $ref: '#/components/responses/Problem'
    patch:
      tags:
      - API endpoints
      summary: "Modify/update an existing Investment Bundler order"
      description: |
        *Partially modify/update an existing `Investment Bundler` order identified by ID*.
        Only `Investment Bundler` order will be replaced that can be accessed by the user (security applied).
        If user does not have access to resource, not found error will be returned instead of unauthorized - to disallow attacks.
      parameters:
      - in: path
        name: doc_invst_bdl_id
        schema:
          type: string
        required: true
        description: ID of the `Investment Bundler` order to modify/update
      - $ref: '#/components/parameters/If-Match'
      - $ref: '#/components/parameters/X-Request-ID'
      - $ref: '#/components/parameters/Accept-Encoding'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/doc_invst_bdl'
      responses:
        '200':
          description: Request successfully processed
          headers:
            ETag:
              $ref: '#/components/headers/ETag'
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                    format: int64
                    example: 123456
                  bdeRecVersion:
                    type: integer
                    format: int64
                    example: 1618390720445656
        'default':
          $ref: '#/components/responses/Problem'


components:
  securitySchemes:
    bearerAuth:            # arbitrary name for the security scheme
      type: http
      scheme: bearer
      bearerFormat: JWT    # optional, arbitrary value for documentation purposes

  schemas:
    code_tab_ref:
      title: code_tab_ref
      type: object
      properties:
        _href:
          description: "Reference/link"
          type: string
          readOnly: true
        id:
          description: "Numeric reference"
          type: integer
          format: int32
        ident:
          description: "Unique symbolic reference"
          type: string
        intlId:
          description: "Symbolic reference"
          type: string
          readOnly: true
    code_tab_ref_action:
      title: code_tab_ref_action
      type: object
      properties:
        _href:
          description: "Reference/link"
          type: string
          readOnly: true
        action:
          enum: [hold, proceed, discard]
          example: hold
          description: "enum reference to action"
          type: string
          writeOnly: true
        id:
          description: "Numeric reference"
          type: integer
          format: int32
        ident:
          description: "Unique symbolic reference"
          type: string
        intlId:
          description: "Symbolic reference"
          type: string
          readOnly: true
    doc_add:
      title: doc_add
      type: object
      properties:
        docAdd:
          description: "Referencing: `/code-doc-adds/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
          x-additionalFieldType: extn_generic
        valList:
          type: array
          items:
            $ref: '#/components/schemas/doc_add_val'
          readOnly: true
          x-additionalFieldType: extn_generic
    doc_add_val:
      title: doc_add_val
      type: object
      properties:
        dateVal:
          type: string
          format: date
          readOnly: true
          x-additionalFieldType: extn_generic
        idVal:
          type: integer
          format: int32
          readOnly: true
          x-additionalFieldType: extn_generic
        nrVal:
          type: number
          readOnly: true
          x-additionalFieldType: extn_generic
        textVal:
          type: string
          readOnly: true
          x-additionalFieldType: extn_generic
        tswtzVal:
          type: string
          format: date-time
          readOnly: true
          x-additionalFieldType: extn_generic
    doc_caldr_day:
      title: doc_caldr_day
      type: object
      properties:
        day:
          type: number
          readOnly: true
        month:
          type: number
          readOnly: true
    doc_caldr_period:
      title: doc_caldr_period
      type: object
      properties:
        annQty:
          type: number
          readOnly: true
        annQtyA:
          type: boolean
          readOnly: true
        eop:
          type: string
          format: date
          readOnly: true
        eopA:
          type: boolean
          readOnly: true
        eopPlan:
          type: string
          format: date
          readOnly: true
        exPeriodDate:
          type: string
          format: date
          readOnly: true
        exPeriodDateA:
          type: boolean
          readOnly: true
        fixDate:
          type: string
          format: date
          readOnly: true
        fixDateA:
          type: boolean
          readOnly: true
        histStatus:
          description: "Referencing: `/code-hist-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        isManual:
          type: boolean
          readOnly: true
        isSusp:
          type: boolean
          readOnly: true
        name:
          type: string
          readOnly: true
        noticeDateFrom:
          type: string
          format: date
          readOnly: true
        noticeDateTo:
          type: string
          format: date
          readOnly: true
        noticeDays:
          type: integer
          format: int32
          readOnly: true
        price:
          type: number
          readOnly: true
        priceA:
          type: boolean
          readOnly: true
        prob:
          type: number
          readOnly: true
        qty:
          type: number
          readOnly: true
        qtyA:
          type: boolean
          readOnly: true
        refDate:
          type: string
          format: date
          readOnly: true
        refNr:
          type: string
          readOnly: true
        sop:
          type: string
          format: date
          readOnly: true
        sopA:
          type: boolean
          readOnly: true
        sopPlan:
          type: string
          format: date
          readOnly: true
        trxDate:
          type: string
          format: date
          readOnly: true
        trxDateA:
          type: boolean
          readOnly: true
        valDate:
          type: string
          format: date
          readOnly: true
        valDateA:
          type: boolean
          readOnly: true
    doc_compo_cf:
      title: doc_compo_cf
      type: object
      properties:
        annQty:
          description: "Annuity Amount"
          type: number
          readOnly: true
        cfAsset:
          description: "Cashflow asset<br>Referencing: `/obj-assets/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        cfCalcMethod:
          description: "Calculation Method<br>Referencing: `/code-cf-calc-methods/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        cfQty:
          description: "Quantity"
          type: number
        compoAfList:
          description: "Asset Flow<br>Referencing: `/code-compo-af-lists/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        compoStruct:
          description: "Component structure<br>Referencing: `/code-cf-structs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        compoType:
          description: "Component type<br>Referencing: `/code-compo-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        dynFixed:
          description: "Dynamisation Fixed Amount"
          type: number
          readOnly: true
        dynPerc:
          description: "Dynamisation %"
          type: number
          readOnly: true
        dynStart:
          description: "Annual Dynamisation Start"
          type: string
          format: date-time-no-tz
          readOnly: true
        endDate:
          description: "End date of cashflow component"
          type: string
          format: date-time-no-tz
          readOnly: true
        factorPeriodRef:
          description: "Reference when to apply the index correction factor<br>Referencing: `/code-period-refs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        fxFactor:
          description: "Fixed exchange rate applied to bond's nominal in case of dual currency bonds"
          type: number
          readOnly: true
        limit:
          description: "Execute Above/on Limit"
          type: number
          readOnly: true
        perc:
          description: "%"
          type: number
          readOnly: true
        prcCaldr:
          $ref: '#/components/schemas/doc_tab_caldr'
        rdmptPhaseList:
          type: array
          items:
            $ref: '#/components/schemas/doc_compo_cf_rdmpt_phase'
          readOnly: true
        seqNr:
          description: "Sequence number"
          type: number
          readOnly: true
        startDate:
          description: "Start date of cashflow component"
          type: string
          format: date-time-no-tz
          readOnly: true
        undrlyObj:
          description: "Underlying object<br>Referencing: `/objs/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
    doc_compo_cf_rdmpt_phase:
      title: doc_compo_cf_rdmpt_phase
      type: object
      properties:
        compoTag:
          description: "Compo Sequence Number"
          type: number
          readOnly: true
        effDate:
          description: "Valid from"
          type: string
          format: date-time-no-tz
          readOnly: true
        val:
          description: "Redemption amount / Redemption in %"
          type: number
          readOnly: true
    doc_cred_chk:
      title: doc_cred_chk
      type: object
      properties:
        credChkEvt:
          description: "The type of credit check event<br>Referencing: `/code-cred-chk-evts/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        newViolAmount:
          description: "The new violation amount related to the credit check event"
          type: number
          readOnly: true
        oldViolAmount:
          description: "The prior violation amount related to the credit check event"
          type: number
          readOnly: true
        refPos:
          description: "The position which was booked by the transaction causing the increase<br>Referencing: `/obj-poss/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        secUser:
          description: "The user involved in the credit check event<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
          readOnly: true
        timestamp:
          description: "The timestamp of the credit check event"
          type: string
          format: date
          readOnly: true
        violPos:
          description: "The position with a violation increase<br>Referencing: `/obj-poss/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
    doc_invst_bdl:
      title: doc_invst_bdl
      type: object
      properties:
        addList:
          type: array
          items:
            $ref: '#/components/schemas/doc_add'
          x-additionalFieldType: extn_generic
        advNr:
          description: "Advice Number of DOC"
          type: integer
          format: int32
        agingId:
          type: integer
          format: int32
          readOnly: true
        bdeRecVersion:
          type: integer
          format: int32
        bdlStatus:
          description: "The execution status of the Investment Bundle.<br>Referencing: `/code-invst-bdl-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        bookDoc:
          description: "Link to the order where the bookings are done<br>Referencing: `/doc-books/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
        bp:
          description: "BP owner of both the Container and MACC the Investment Bundler will take place on<br>Referencing: `/obj-bps/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        bu:
          description: "Business unit of order itself<br>Referencing: `/obj-bps/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        cont:
          description: "Container the Investment Bundler will take place on<br>Referencing: `/obj-conts/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        contA:
          description: "Auto/Manual flag of Container the Investment Bundler will take place on"
          type: boolean
          readOnly: true
        credChkList:
          type: array
          items:
            $ref: '#/components/schemas/doc_cred_chk'
          readOnly: true
        discdReason:
          description: "The reason for discarding this order<br>Referencing: `/code-doc-discd-reasons/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docBlock:
          description: "Block Code<br>Referencing: `/code-blocks/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docBlockA:
          description: "Block Code"
          type: boolean
        docPrcInfo:
          description: "process info id<br>Referencing: `/code-doc-prc-infos/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docPrcInfoRemark:
          description: "process info remark"
          type: string
        docRevsCause:
          description: "Reversal Cause<br>Referencing: `/code-doc-revs-causes/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docRole:
          description: "Order role<br>Referencing: `/code-doc-roles/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        dueDate:
          description: "Due Date"
          type: string
          format: date
        duplDoc:
          description: "The original order of which this one is a duplicate of<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
        enteredVmDate:
          description: "date order entered visum monitoring"
          type: string
          format: date
        expirDate:
          description: "Expiry date"
          type: string
          format: date
        extlRefNr:
          description: "External reference number"
          type: string
        extn:
          $ref: '#/components/schemas/doc_invst_bdl_extn'
        feBp:
          description: "Broker business partner<br>Referencing: `/obj-bps/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        hasPostit:
          description: "Has order a post-it"
          type: boolean
        hasWorkItem:
          type: boolean
          readOnly: true
        id:
          description: "ID<br>Referencing: `/docs/{id}`"
          type: integer
          format: int64
        initLotBal:
          description: "The main initial lot balance, necessarily calculated from the rules."
          type: number
          readOnly: true
        initLotBuf:
          description: "Buffer lot balance that will act as an additional amount to the main lot."
          type: number
          readOnly: true
        initLotCmtd:
          description: "Method used to calculate the Bundle's initial lot.<br>Referencing: `/code-invst-bdl-init-lot-cmtds/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        inpr:
          allOf:
            - $ref: '#/components/schemas/docp_inpr'
          readOnly: true
        insBySecUser:
          description: "Order created by user<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
        intlRefNr:
          description: "Internal reference number"
          type: string
        isBackdt:
          description: "Is backdated transaction"
          type: boolean
        isDel:
          description: "is order deleted"
          type: boolean
        isFinal:
          description: "Is order in a final status?"
          type: boolean
        isMigr:
          description: "Is migration transaction"
          type: boolean
          readOnly: true
        isRevs:
          description: "Is reversal transaction (reversal w/o original)"
          type: boolean
        isSim:
          description: "Is the order a simulation?"
          type: boolean
          readOnly: true
        isaRelv:
          description: "ISA relevant"
          type: boolean
        keyList:
          type: array
          items:
            $ref: '#/components/schemas/doc_key'
          x-additionalFieldType: extn_generic
        lastMsg:
          description: "Last message ID<br>Referencing: `/msgs/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        lastTrans:
          description: "Last transition"
          type: string
          format: date-time
        lastTransSecUser:
          description: "Last transition user<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
          readOnly: true
        lastUpdTimestamp:
          description: "The start time of last point a bundle was executed by the update task."
          type: string
          format: date
          readOnly: true
        macc:
          description: "Money Account the Investment Bundler instance will take place on<br>Referencing: `/obj-poss/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        masterDoc:
          description: "Return the master doc of the current doc<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
        medium:
          description: "Communication medium, e.g. phone, fax, etc.<br>Referencing: `/code-mediums/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        metaTyp:
          description: "Meta type<br>Referencing: `/meta-typs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        nextRuleId:
          description: "Next unique ID to a rule. Always incremented with each rule."
          type: integer
          format: int32
          readOnly: true
        notBonusRelv:
          description: "not bonus relevant"
          type: boolean
        notBonusWidrwRelv:
          description: "not bonus withdrawal relevant"
          type: boolean
        notReconStornoRelv:
          description: "Not Reconciliation Storno Relevant"
          type: boolean
        notWidrwRelv:
          description: "not withdrawal relevant"
          type: boolean
        orderDate:
          description: "Order date"
          type: string
          format: date
        orderDateTz:
          description: "Order date"
          type: string
          format: date-time
          readOnly: true
        orderNr:
          description: "Reference no."
          type: integer
          format: int32
        orderType:
          description: "Order type<br>Referencing: `/code-order-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        orderTypeA:
          description: "Auto/Manual flag of Order type"
          type: boolean
          readOnly: true
        orderTypeGrp:
          description: "Order type group<br>Referencing: `/code-order-type-grps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        orderedBy:
          description: "Ordered by"
          type: string
        origDoc:
          description: |
            The original order of which this one is a rectificate of<br>
            Referencing: `/doc-invst-bdls/{id}`
          allOf:
            - $ref: '#/components/schemas/doc_ref'
          readOnly: true
        overrideList:
          type: array
          items:
            $ref: '#/components/schemas/doc_override'
        perfDate:
          description: "Performance date"
          type: string
          format: date
        perfDateA:
          description: "Is performance date set automatically"
          type: boolean
        postitList:
          type: array
          items:
            $ref: '#/components/schemas/doc_postit'
        rectiDoc:
          description: |
            **_Only visible within compound APIs_**<br>
            The rectificate of this order.<br>
            Referencing: `/doc-invst-bdls/{id}`<br>
            Back-property: origDoc
          allOf:
            - $ref: '#/components/schemas/doc_ref'
          readOnly: true
        recur:
          $ref: '#/components/schemas/docp_recur'
        refDoc:
          description: "Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
          readOnly: true
        remnBaccBalMax:
          description: "User-specified maximum threshold above BACC target."
          type: number
        remnBaccBalMin:
          description: "User-specified minimum threshold below BACC target."
          type: number
        respBp:
          description: "Responsible Business Partner<br>Referencing: `/obj-bps/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        respBpA:
          description: "Responsible Business Partner a/m"
          type: boolean
        respObj:
          description: "Responsible Object<br>Referencing: `/objs/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        respObjA:
          description: "Responsible Object a/m"
          type: boolean
        revsOfDoc:
          description: "The original order for which the actual order is the reversal<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
        revsOriginUser:
          description: "Originator<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
        ruleList:
          type: array
          items:
            $ref: '#/components/schemas/docp_invst_bdl_rule'
        sdsInstn:
          description: "SDS instance ID<br>Referencing: `/code-extl-syss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        settlePlan:
          description: "Settlement plan<br>Referencing: `/code-settle-plans/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        settlePlanA:
          description: "Settlement plan"
          type: boolean
        terminal:
          description: "Terminal where advice of order was printed<br>Referencing: `/obj-terminals/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        todoTempl:
          description: "ToDo Template<br>Referencing: `/code-todo-templs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        transList:
          type: array
          items:
            $ref: '#/components/schemas/doc_trans'
        trgBal:
          description: "Target balance for an Investment Bundle"
          type: number
          readOnly: true
        trgBalType:
          description: "Target balance type for an Investment Bundle<br>Referencing: `/code-invst-bdl-trg-bal-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        trxDate:
          description: "Transaction date"
          type: string
          format: date
        valDate:
          description: "Value date"
          type: string
          format: date
        wfcAction:
          description: "Workflow Action<br>Referencing: `/wfc-action-invst_bdls/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref_action'
        wfcStatus:
          description: "Workflow  status<br>Referencing: `/wfc-status-invst_bdls/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        workItemList:
          type: array
          items:
            $ref: '#/components/schemas/doc_tab_work_item'
          readOnly: true
    doc_invst_bdl_extn:
      title: doc_invst_bdl_extn
      type: object
      properties:
        afsDocId:
          description: "AFS Order ID"
          type: string
        afsExtlRefNr:
          description: "AFS external reference number"
          type: string
        api1RefNr:
          description: "Primary API external reference number"
          type: string
        api2RefNr:
          description: "Secondary API external reference number"
          type: string
        dxsCtxId:
          description: "DXS context ID"
          type: string
        dxsEntityExtlRef:
          description: "DXS entity external reference"
          type: string
        mba$authEdtRvwCompLvList:
          description: "Authorization: comp lv that need to authorize for specific edt rvw in jtg"
          type: array
          items:
            type: string
        mba$authorEdtRvwJtgList:
          type: array
          items:
            $ref: '#/components/schemas/number'
        mba$blockAprvUserList:
          description: "List of Users having approved the Compliance Blocking Codes<br>Referencing: `/users/{id}`"
          type: array
          items:
            $ref: '#/components/schemas/user_ref'
        mba$blockJtgList:
          description: "Current List of matching compliance block IDs with the corresponding JTG IDs"
          type: string
        mba$blockRemnJtgList:
          description: "List of JTG still required to approve the Compliance Blocking Codes<br>Referencing: `/mba$code-job-type-grps/{id}`"
          type: array
          items:
            $ref: '#/components/schemas/code_tab_ref'
        mba$ccPastAuthorUserList:
          description: "Authorization: User List for current jtg (Credit Check)<br>Referencing: `/users/{id}`"
          type: array
          items:
            $ref: '#/components/schemas/user_ref'
        mba$credChkAuthCompLvList:
          description: "Authorization: comp lv that need to authorize in jtg (Credit Check)"
          type: array
          items:
            type: string
        mba$credChkAuthorJtgList:
          description: "JTG to authorize the order. Indication, which jtg has to author (Credit Check).<br>Referencing: `/mba$code-job-type-grps/{id}`"
          type: array
          items:
            $ref: '#/components/schemas/code_tab_ref'
        mba$credChkCmt:
          description: "Comment of the Decision (Credit Check)"
          type: array
          items:
            type: string
        mba$credChkRestr:
          description: "Additional Restriction List (Credit Check)"
          type: array
          items:
            type: string
        mba$docErrType:
          description: "Order Processing Error Type<br>Referencing: `/mba$code-doc-err-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$edtRvwJtgList:
          type: array
          items:
            $ref: '#/components/schemas/number'
        mba$edtRvwUserList:
          description: "Last user to edited the order for jtg (as listed in mba$edt_rvw_jtg_list)<br>Referencing: `/users/{id}`"
          type: array
          items:
            $ref: '#/components/schemas/user_ref'
        mba$iAuthEdtRvwCompLvLst:
          description: "Authorization:initial comp lv that need to authorize for specific edt rvw in jtg"
          type: array
          items:
            type: string
        mba$iCredChkAuthCompLvLt:
          description: "Authorization:initial comp lv that need to authorize in jtg (Credit Check)"
          type: array
          items:
            type: string
        mba$instrDet:
          description: "Instruction Details"
          type: string
        mba$isDfltValSet:
          description: "Have default values been applied"
          type: boolean
        mba$lswfInsJtg:
          description: "Job Type Group that inserted the order<br>Referencing: `/mba$code-job-type-grps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfJtg:
          description: "Current job type group owning the status<br>Referencing: `/mba$code-job-type-grps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfLastSwfIntfWfaId:
          description: "Last interface action used"
          type: number
        mba$lswfLastSwfWfaId:
          description: "Last workflow action used"
          type: number
        mba$lswfLastSwfWfsId:
          description: "Communication to next department"
          type: number
        mba$lswfNextJtg:
          description: "Next job type group owning the status<br>Referencing: `/mba$code-job-type-grps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfNextJtgInfo:
          description: "Communication to next job type group"
          type: string
        mba$lswfNextRa:
          description: "Next Routing Action for Linkable Sub-Workflow<br>Referencing: `/mba$code-lswf-raps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfNextSwf:
          description: "Next Linkable Sub-Workflow in Workflow<br>Referencing: `/mba$code-lswfs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfPrevSwf:
          description: "Previous Linkable Sub-Workflow in Workflow<br>Referencing: `/mba$code-lswfs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfRa:
          description: "Current Routing Action for Linkable Sub-Workflow<br>Referencing: `/mba$code-lswf-raps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfSwf:
          description: "Current Linkable Sub-Workflow in Workflow<br>Referencing: `/mba$code-lswfs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$lswfUc:
          description: "Use Case assigned to the order<br>Referencing: `/mba$code-lswf-ucs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$mediumA:
          description: "Medium A/M flag"
          type: boolean
        mba$mediumPhoneNr:
          description: "Reference phone number to be registered in case of a phone call"
          type: string
        mba$pastAuthorUserList:
          description: "Authorization: User List for current jtg<br>Referencing: `/users/{id}`"
          type: array
          items:
            $ref: '#/components/schemas/user_ref'
        mba$srcFund:
          description: "Source of Funds<br>Referencing: `/mba$code-src-funds/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        mba$templAsset:
          description: "Template Asset<br>Referencing: `/obj-assets/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        pkpSvcRsvIdent:
          description: "Position Keeping Service: Reservation/advice identifier"
          type: string
    doc_invst_bdls:
      title: List of doc_invst_bdl
      type: array
      items:
        $ref: '#/components/schemas/doc_invst_bdl'
      uniqueItems: true
    doc_key:
      title: doc_key
      type: object
      properties:
        docKey:
          description: "Referencing: `/code-doc-keys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
          x-additionalFieldType: extn_generic
        val:
          type: string
          readOnly: true
          x-additionalFieldType: extn_generic
    doc_override:
      title: doc_override
      type: object
      properties:
        override:
          description: "Override code<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        overrideComment:
          description: "Comment<br>Referencing: `/code-override-comments/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        overrideText:
          description: "Override text"
          type: string
        secUser:
          description: "User<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
        signt:
          description: "Signature"
          type: string
        timestamp:
          description: "Timestamp"
          type: string
          format: date-time
    doc_postit:
      title: doc_postit
      type: object
      properties:
        ctx:
          description: "Text<br>Referencing: `/code-postit-ctxs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        doProp:
          description: "Is postit propagated"
          type: boolean
        isMan:
          description: "Is manualally entered"
          type: boolean
        role:
          description: "Role<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
        subType:
          description: "Subtype<br>Referencing: `/code-postit-sub-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        text:
          description: "Text"
          type: string
        timestamp:
          description: "Timestamp"
          type: string
          format: date-time-no-tz
        topic:
          description: "Topic"
          type: string
        type:
          description: "Type<br>Referencing: `/code-postit-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        user:
          description: "Inserted by<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
    doc_ref:
      title: doc_ref
      type: object
      properties:
        _href:
          description: "Reference/link"
          type: string
          readOnly: true
        _ident:
          description: "Only used for workItem use cases, cross-reference in workItem context."
          type: string
          writeOnly: true
        id:
          description: "Numeric reference"
          type: integer
          format: int64
    doc_tab_caldr:
      title: doc_tab_caldr
      type: object
      properties:
        bindDateList:
          type: string
          readOnly: true
        dayList:
          type: array
          items:
            $ref: '#/components/schemas/doc_caldr_day'
          readOnly: true
        endDate:
          type: string
          format: date
        exPeriodOffsetExpr:
          description: "Ex-Date Offset"
          type: string
          readOnly: true
        exclLastBrkPeriod:
          type: boolean
        fixDateCaldrList:
          type: string
          readOnly: true
        fixDateOffset:
          description: "Referencing: `/code-date-offsets/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        fixDateOffsetExpr:
          type: string
          readOnly: true
        fixPeriodRef:
          description: "Referencing: `/code-period-refs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        freq:
          type: number
        isPeriodBind:
          type: boolean
          readOnly: true
        isUpfront:
          type: boolean
        name:
          type: string
          readOnly: true
        period:
          description: "Referencing: `/code-pay-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        periodCnt:
          type: number
        periodEnd:
          type: string
          format: date
        periodEndA:
          type: boolean
          readOnly: true
        periodList:
          type: array
          items:
            $ref: '#/components/schemas/doc_caldr_period'
          readOnly: true
        periodStart:
          type: string
          format: date
        periodStartA:
          type: boolean
          readOnly: true
        periodicity:
          type: string
        refDateCaldrList:
          type: string
          readOnly: true
        refDateOffset:
          description: "Referencing: `/code-date-offsets/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        refDateOffsetExpr:
          type: string
          readOnly: true
        startDate:
          type: string
          format: date
        trxDateCaldrList:
          type: string
          readOnly: true
        trxDateOffset:
          description: "Referencing: `/code-date-offsets/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        trxDateOffsetExpr:
          type: string
          readOnly: true
        valDateCaldrList:
          type: string
          readOnly: true
        valDateOffset:
          description: "Referencing: `/code-date-offsets/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        valDateOffsetExpr:
          type: string
          readOnly: true
    doc_tab_work_item:
      title: doc_tab_work_item
      type: object
      properties:
        closeDate:
          description: "Close Date"
          type: string
          format: date
        descn:
          description: "Description"
          type: string
        isSlaveDirty:
          description: "Is dirty ?"
          type: boolean
        isTightLink:
          description: "Is Linked"
          type: boolean
        masterDoc:
          description: "Master Order<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
        seqNr:
          description: "Sequence no."
          type: integer
          format: int32
        slaveDoc:
          description: "Order<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
        subject:
          description: "Subject"
          type: string
        tag:
          description: "Tag"
          type: string
        workItemCtx:
          description: "Work Item Context<br>Referencing: `/code-work-item-ctxs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        workItemStatus:
          description: "Work Item Status<br>Referencing: `/code-work-item-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        workItemType:
          description: "Work Item Type<br>Referencing: `/code-work-item-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
    doc_trans:
      title: doc_trans
      type: object
      properties:
        calcCnt:
          type: integer
          format: int32
        calcTime:
          type: integer
          format: int32
        intfWfcAction:
          description: "Workflow Interface Action<br>Referencing: `/wfc-action-invst_bdls/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        newWfcStatus:
          description: "Workflow Status<br>Referencing: `/wfc-status-invst_bdls/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        oldWfcStatus:
          description: "Workflow Status<br>Referencing: `/wfc-status-invst_bdls/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        opnTime:
          type: integer
          format: int32
        remark:
          description: "Remark"
          type: string
        ruleTime:
          type: integer
          format: int32
        secUser:
          description: "User<br>Referencing: `/users/{id}`"
          allOf:
            - $ref: '#/components/schemas/user_ref'
        seqNr:
          description: "Sequence no."
          type: integer
          format: int32
        timestamp:
          description: "Timestamp"
          type: string
          format: date-time
        transAction:
          description: "Transition<br>Referencing: `/code-trans-actions/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        userTime:
          type: integer
          format: int32
        wfcAction:
          description: "Workflow Action<br>Referencing: `/wfc-action-invst_bdls/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        wfcTime:
          type: integer
          format: int32
    docp_inpr:
      title: docp_inpr
      type: object
      properties:
        advsClientList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_client'
        advsLoc:
          description: "Advisor location at the time of advice.<br>Referencing: `/obj-countrys/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        advsLocA:
          description: "Auto/Manual flag of Advisor location at the time of advice."
          type: boolean
          readOnly: true
        busiCtx:
          description: |
            The business context in which the rules are to
                      be evaluated.<br>Referencing: `/code-invstr-prot-busi-ctxs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        cont:
          description: |
            The container under discussion. The owner of
                      that container is also used for filtering the person lookup for
                      'advs_client_list.person_id'.<br>Referencing: `/obj-conts/{id}`
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        doc:
          description: "Doc_id<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
          readOnly: true
        dur:
          description: "Duration of the meeting.<br>Referencing: `/code-invstr-prot-durs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        durA:
          description: "Auto/Manual flag of Duration of the meeting."
          type: boolean
          readOnly: true
        finalVersionSeqNr:
          description: "The sequence number of the final version."
          type: integer
          format: int32
          readOnly: true
        frze:
          description: |
            Set to TRUE (+) in order to freeze the rules and
                      the fields; FALSE in order to have the rules be re-evaluted upon changes
                      in the order.
          type: boolean
          readOnly: true
        intive:
          description: "Initiative: client or investment advisor.<br>Referencing: `/code-invstr-prot-intives/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        intiveA:
          description: "Auto/Manual flag of Initiative: client or investment advisor."
          type: boolean
          readOnly: true
        invstAdvs:
          description: |
            Investment advisor - either a business partner
                      (BP) or person, depending on the organisational structure activation.<br>Referencing: `/objs/{id}`
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        invstAdvsA:
          description: |
            Auto/Manual flag of Investment advisor - either a business partner
                      (BP) or person, depending on the organisational structure activation.
          type: boolean
          readOnly: true
        ips:
          description: "The selected investment policy statement (IPS).<br>Referencing: `/obj-ipss/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        meetingDate:
          description: |
            Date and time of the meeting, converted to local
                      time zone.
          type: string
          format: date-time-no-tz
        meetingDateA:
          description: |
            Auto/Manual flag of Date and time of the meeting, converted to local
                      time zone.
          type: boolean
          readOnly: true
        overallStatus:
          description: "The overall investor protection status of the final version.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        refDoc:
          description: |
            Reference order for which the investor
                      protection rules have already been evaluated and which created this
                      order (to which this investor protection part is attached).<br>Referencing: `/docs/{id}`
          allOf:
            - $ref: '#/components/schemas/doc_ref'
          readOnly: true
        scusList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_cus'
        scusOverStatus:
          description: "Overall service custom teststatus.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        sdocmOverStatus:
          description: |
            Overall service information document status
                      (okay, restricted, not okay).<br>Referencing: `/code-invstr-prot-statuss/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        sindList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_indu'
        sindOverStatus:
          description: "Overall service information duty status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        sindmOverState:
          description: |
            Overall service information document state
                      (required, received, handed over, ...).<br>Referencing: `/code-invstr-prot-docm-states/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        svcDocmList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_docm'
        svcList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_svc'
        svcOverStatus:
          description: "Overall advisory service status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        svcType:
          description: |
            The provided bank service respective degree of
                      advice given: Advice Given, Opinion Given, Direct Execution, ...<br>Referencing: `/code-invstr-prot-svc-types/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        svcTypeA:
          description: |
            Auto/Manual flag of The provided bank service respective degree of
                      advice given: Advice Given, Opinion Given, Direct Execution, ...
          type: boolean
          readOnly: true
        versList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_vers'
    docp_inpr_apof:
      title: docp_inpr_apof
      type: object
      properties:
        legalNorm:
          description: "Legal norm for which this applicable offering is evaluated.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "Applicable offering override code.<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: "Justification template for the override of the applicable offering status.<br>Referencing: `/code-invstr-prot-justifs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrJustifTemplA:
          description: "Auto/Manual flag of Justification template for the override of the applicable offering status."
          type: boolean
          readOnly: true
        ovrJustifText:
          description: "Justification text for the override of the applicable offering status."
          type: string
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: "Advised client for whom this applicable offering is evaluated.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        replyOvr:
          description: "Set to TRUE in order to override the 'apof_rule_reply_id'; valid 'apof_ovr_id' required."
          type: boolean
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleMsgKeyOrVal:
          description: |
            The encoded, translatable text (or the
                      non-translatable text itself) for the message explaining the (negative)
                      applicable offering reply. Use 'rule_msg' or alternatively
                      bre_text.text#text in order to display the (translated) text.
          type: string
          readOnly: true
        ruleReply:
          description: "The reply of the applicable offering rule service.<br>Referencing: `/code-invstr-prot-rule-replys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        status:
          description: "Applicable offering status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_asset:
      title: docp_inpr_asset
      type: object
      properties:
        acusList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_cus'
        acusOverStatus:
          description: "Overall asset custom test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        advReasonTempl:
          description: "Template for the advice reason.<br>Referencing: `/code-invstr-prot-adv-reasons/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        advReasonTemplA:
          description: "Auto/Manual flag of Template for the advice reason."
          type: boolean
          readOnly: true
        advReasonText:
          description: "The reason for the advice."
          type: string
        aindList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_indu'
        aindOverStatus:
          description: "Overall asset information duty status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        apofList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_apof'
        apofOverStatus:
          description: "Overall asset applicable offering status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        asset:
          description: "The asset being discussed.<br>Referencing: `/obj-assets/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        assetComment:
          description: "Asset comment."
          type: string
        atstList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_atst'
        atstOverStatus:
          description: "Overall appropriateness status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        docmList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_docm'
        docmOverStatus:
          description: |
            Overall asset information document status
                      (okay, restricted, not okay).<br>Referencing: `/code-invstr-prot-statuss/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        indmOverState:
          description: |
            Overall asset information document state
                      (required, received, handed over, ...).<br>Referencing: `/code-invstr-prot-docm-states/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        mkt:
          description: "The market on which the asset is going to be traded.<br>Referencing: `/obj-mkts/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        optimInstr:
          description: "The optimiser instruction which has been set for the position on the actual order (e.g. REBALP).<br>Referencing: `/code-optim-instrs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        pos:
          description: "The position which is on either buy, hold or sell.<br>Referencing: `/obj-poss/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        qty:
          description: "The quantity by which the position is changed: positive for buy, negative for sell."
          type: number
          readOnly: true
        remark:
          description: "The remark which has been added to the position on the actual order (e.g. REBALP)."
          type: string
          readOnly: true
        solic:
          description: "Solicitation: whether the discussed asset is recommended or on client request.<br>Referencing: `/code-invstr-prot-solics/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
    docp_inpr_atst:
      title: docp_inpr_atst
      type: object
      properties:
        existKnowlExper:
          description: "The existing knowledge and experience (K&E) of the client as a comma separated list."
          type: string
          readOnly: true
        legalNorm:
          description: "Legal norm for which this appropriateness test is evaluated.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "A-test override code.<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: "Justification template for the override of the a-test status.<br>Referencing: `/code-invstr-prot-justifs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrJustifTemplA:
          description: "Auto/Manual flag of Justification template for the override of the a-test status."
          type: boolean
          readOnly: true
        ovrJustifText:
          description: "Justification text for the override of the a-test status."
          type: string
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: "Advised client for which this appropriateness test is evaluated.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        replyOvr:
          description: "Set to TRUE in order to override the 'rule_reply_id'; valid 'ovr_id' required."
          type: boolean
        requKnowlExper:
          description: "The required knowledge and experience (K&E) for the asset (in the parent ASSET part)."
          type: string
          readOnly: true
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleReply:
          description: "The reply of the a-test rule service.<br>Referencing: `/code-invstr-prot-rule-replys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        status:
          description: "A-test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        succ:
          description: "Indicates whether the client has successfully passed the a-test (true) or not (false)."
          type: boolean
          readOnly: true
    docp_inpr_client:
      title: docp_inpr_client
      type: object
      properties:
        involv:
          description: "Client involvement: mandatory or optional.<br>Referencing: `/code-invstr-prot-involvs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        legalNormList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_legal'
        loc:
          description: "Client location at the time of advice.<br>Referencing: `/obj-countrys/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        manual:
          description: "Indicates whether the client was manually added (TRUE) or due to the stakeholder nomination rule (FALSE)."
          type: boolean
          readOnly: true
        medium:
          description: "The medium (Face to Face, Phone, E-Mail, ...) by which the service is provided to this client.<br>Referencing: `/code-invstr-prot-mediums/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: "Advised client.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        role:
          description: "Client role.<br>Referencing: `/code-invstr-prot-roles/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_cobe:
      title: docp_inpr_cobe
      type: object
      properties:
        legalNorm:
          description: "Legal norm for which this cost-benefit test is evaluated.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "Cost-benefit test override code.<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: |
            Justification template for the override of the
                      cost-benefit test status.<br>Referencing: `/code-invstr-prot-justifs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifText:
          description: |
            Justification text for the override of the
                      cost-benefit test status.
          type: string
          readOnly: true
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: "Advised client for which this cost-benefit test is evaluated.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        replyOvr:
          description: |
            Set to TRUE in order to override the
                      'cobe_rule_reply_id'; valid 'cobe_ovr_id' required.
          type: boolean
          readOnly: true
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleMsgCtx:
          description: |
            The context which provides the parameters,
                      separated by semicolon (INVSTR_PROT_CONST.C_CTX_SEP), for the message
                      placeholders (&1, &2 etc.) in 'rule_msg_key_or_val'.
          type: string
          readOnly: true
        ruleMsgKeyOrVal:
          description: |
            The OUT_TEXT_NORM key (or the non-translatable
                      text itself) for the message explaining the (negative) cost-benefit test
                      reply.  Use 'rule_msg' in order to display the (translated) text.
          type: string
          readOnly: true
        ruleReply:
          description: "The reply of the cost-benefit test rule service (CSS function).<br>Referencing: `/code-invstr-prot-rule-replys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        status:
          description: "Cost-benefit test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_cus:
      title: docp_inpr_cus
      type: object
      properties:
        legalNorm:
          description: "Legal norm for which this custom test is evaluated.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "Custom test override code.<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: |
            Justification template for the override of the
                      custom test status.<br>Referencing: `/code-invstr-prot-justifs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrJustifTemplA:
          description: |
            Auto/Manual flag of Justification template for the override of the
                      custom test status.
          type: boolean
          readOnly: true
        ovrJustifText:
          description: |
            Justification text for the override of the
                      custom test status.
          type: string
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: "Advised client for which this custom test is evaluated.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        replyOvr:
          description: |
            Set to TRUE in order to override the
                      'cobe_rule_reply_id'; valid 'cobe_ovr_id' required.
          type: boolean
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleMsgCtx:
          description: |
            The context which provides the parameters,
                      separated by semicolon (INVSTR_PROT_CONST.C_CTX_SEP), for the message
                      placeholders (&1, &2 etc.) in 'rule_msg_key_or_val'.
          type: string
          readOnly: true
        ruleMsgKeyOrVal:
          description: |
            The OUT_TEXT_NORM key (or the non-translatable
                      text itself) for the message explaining the (negative) custom test
                      reply.  Use 'rule_msg' in order to display the (translated) text.
          type: string
          readOnly: true
        ruleReply:
          description: "The reply of the custom test rule service (CSS function).<br>Referencing: `/code-invstr-prot-rule-replys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        status:
          description: "Custom test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_docm:
      title: docp_inpr_docm
      type: object
      properties:
        altLangList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_lang'
          readOnly: true
        avlLangList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_lang'
          readOnly: true
        cltConf:
          description: |
            Indicates whether the client has confirmed
                      or rejected the information document.<br>Referencing: `/code-invstr-prot-clt-confs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docmDistr:
          description: |
            Indicates the way the document is to be
                      distributed, specifically whether it is mandatory or optional, or whether
                      it is to be retrieved manually and personally handed over.<br>Referencing: `/code-invstr-prot-docm-distrs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docmState:
          description: |
            Information document state (Required, Received,
                      Handed Over, ...).<br>Referencing: `/code-invstr-prot-docm-states/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        docmType:
          description: |
            The type of the information document,
                      for instance PIB or PRIIP-KID. The actually received document type is
                      stored in 'rcvd_docm_type_id'.<br>Referencing: `/code-info-docm-types/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        jusCountry:
          description: |
            The jurisdiction for which the information
                      document is to be generated.<br>Referencing: `/obj-countrys/{id}`
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        lang:
          description: |
            The language in which the information document
                      is to be generated.<br>Referencing: `/code-langs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        legalNorm:
          description: |
            The Legal norm for which the information document
                      is required.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        manual:
          description: |
            Indicates whether the information document was
                      manually added (TRUE) or due to the information duty rule (FALSE).
          type: boolean
          readOnly: true
        ovDocmState:
          description: |
            Overall information document state (Required,
                      Received, Handed Over, ...) which takes the 'docm_state_id' and the
                      'clt_conf_id' into account.<br>Referencing: `/code-invstr-prot-docm-states/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "Information document override code.<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: |
            Justification template for the override of the
                      information document status.<br>Referencing: `/code-invstr-prot-justifs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrJustifTemplA:
          description: |
            Auto/Manual flag of Justification template for the override of the
                      information document status.
          type: boolean
          readOnly: true
        ovrJustifText:
          description: |
            Justification text for the override of the
                      information document status.
          type: string
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: |
            Advised client for which the information document
                      is required.<br>Referencing: `/obj-persons/{id}`
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        postTrade:
          description: |
            Indicates whether the document can be delivered
                      post-trade or not.<br>Referencing: `/code-invstr-prot-post-trades/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        prefLang:
          description: |
            The preferred language which has been determined
                      by the information document detail rule service.<br>Referencing: `/code-langs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        rcvdDocmType:
          description: |
            The received type of the information document,
                      for instance PIB or PRIIP-KID. In most cases this is expected to be
                      equal to 'docm_type_id', except if the requested document type could
                      not be retrieved and alternative documents have been specified.<br>Referencing: `/code-info-docm-types/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        rcvdLang:
          description: |
            The received language of the information
                      document. In most cases this is expected to be equal to 'lang_id', except
                      if the requested document language could not be retrieved and
                      alternative languages have been specified.<br>Referencing: `/code-langs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        requ:
          description: |
            Indicates whether the information document needs
                      to be retrieved or not.
          type: boolean
        rtrvOvr:
          description: "Information document retrieval override checkbox."
          type: boolean
        rtrvTime:
          description: |
            Information document retrieval date and time,
                      converted to local time zone.
          type: string
          format: date-time-no-tz
          readOnly: true
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleCltConf:
          description: |
            Indicates whether a client confirmation for
                      the information document is required or not.<br>Referencing: `/code-invstr-prot-clt-confs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDocmDistr:
          description: |
            The distribution which has been determined by
                      the information duty rule service. Also refer to 'docm_distr_id'.<br>Referencing: `/code-invstr-prot-docm-distrs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleJusCountry:
          description: |
            The jurisdiction which has been determined by
                      the information document detail rule service.<br>Referencing: `/obj-countrys/{id}`
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        statusMsgCtx:
          description: |
            The context which provides the parameters,
                      separated by semicolon (INVSTR_PROT_CONST.C_CTX_SEP), for the message
                      placeholders (&1, &2 etc.) in 'status_msg_key_val'.
          type: string
          readOnly: true
        statusMsgKeyVal:
          description: |
            The OUT_TEXT_NORM key (or the non-translatable
                      text itself) for the information document retrieval status
                      message. Use 'status_msg' in order to display the (translated) text.
          type: string
          readOnly: true
        uuid:
          description: |
            Information document universally unique
                      identifier (UUID).
          type: string
          readOnly: true
    docp_inpr_indu:
      title: docp_inpr_indu
      type: object
      properties:
        agingId:
          description: "ID"
          type: number
          readOnly: true
        doc:
          description: "Doc_id<br>Referencing: `/docs/{id}`"
          allOf:
            - $ref: '#/components/schemas/doc_ref'
          readOnly: true
        docm:
          description: "Required client document.<br>Referencing: `/obj-docms/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        docmDistr:
          description: |
            Defines the information document distribution,
                      for instance mandatory, optional or post-trade delivery.<br>Referencing: `/code-invstr-prot-docm-distrs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        docmExists:
          description: |
            Indicates whether the required client document
                      exists (true) or not (false).
          type: boolean
          readOnly: true
        docmOwns:
          description: |
            Ownership of the required client document:
                      person, BP, CONT, IPS.<br>Referencing: `/code-invstr-prot-docm-ownss/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        docmType:
          description: "The information document type (e.g. PIB or KIID).<br>Referencing: `/code-info-docm-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        docmValid:
          description: |
            Indicates whether the required client document
                      has a valid signature (signed, not expired).
          type: boolean
          readOnly: true
        infoDuty:
          description: |
            The kind of information duty: information
                      document, client document or disclaimer (text).<br>Referencing: `/code-invstr-prot-info-dutys/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        legalNorm:
          description: |
            Legal norm for which this asset information duty
                      is required.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "Information duty override code<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: |
            Justification template for the override of the
                      information duty status.<br>Referencing: `/code-invstr-prot-justifs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrJustifTemplA:
          description: |
            Auto/Manual flag of Justification template for the override of the
                      information duty status.
          type: boolean
          readOnly: true
        ovrJustifText:
          description: |
            Justification text for the override of the
                      information duty status.
          type: string
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: |
            Advised client for whom this asset information
                      duty is required.<br>Referencing: `/obj-persons/{id}`
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        postTrade:
          description: |
            Indicates whether the document can be delivered
                      post-trade or not.<br>Referencing: `/code-invstr-prot-post-trades/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        replyOvr:
          description: |
            Set to TRUE in order to override the
                      'docm_exists', 'docm_valid' result; valid 'ovr_id' required.
          type: boolean
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleMsgKeyOrVal:
          description: |
            The encoded, translatable text (or the
                      non-translatable text itself) according to 'info_duty_id', for instance
                      a disclaimer text or an explanation why a given information document is
                      required. Use 'rule_msg' or alternatively bre_text.text#text in order to
                      display the (translated) text.
          type: string
          readOnly: true
        status:
          description: "Information duty status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_lang:
      title: docp_inpr_lang
      type: object
      properties:
        lang:
          description: "The ID of the language.<br>Referencing: `/code-langs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        partId:
          description: "Part ID"
          type: number
          readOnly: true
    docp_inpr_legal:
      title: docp_inpr_legal
      type: object
      properties:
        aTest:
          description: "An appropiateness test is needed when set to TRUE."
          type: boolean
          readOnly: true
        apofTest:
          description: "Applicable offering test is needed when set to TRUE."
          type: boolean
          readOnly: true
        cbTest:
          description: "A cost-benefit test is needed when set to TRUE."
          type: boolean
          readOnly: true
        customTest:
          description: "A custom test is needed when set to TRUE."
          type: boolean
          readOnly: true
        induTest:
          description: "Information duty is needed when set to TRUE."
          type: boolean
          readOnly: true
        legalNorm:
          description: "Legal Norm.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        sTest:
          description: "A suitability test is needed when set to TRUE."
          type: boolean
          readOnly: true
    docp_inpr_restr:
      title: docp_inpr_restr
      type: object
      properties:
        restr:
          description: "The restriction which implements the s-test.<br>Referencing: `/obj-restrs/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        status:
          description: "S-test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_stst:
      title: docp_inpr_stst
      type: object
      properties:
        ststLegalNorm:
          description: "Legal norm for which this suitability test is evaluated.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ststPerson:
          description: "Advised client for which this suitability test is evaluated.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        ststRestr:
          description: "The restriction which implements the s-test.<br>Referencing: `/obj-restrs/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
    docp_inpr_svc:
      title: docp_inpr_svc
      type: object
      properties:
        legalNorm:
          description: "Legal norm for which this advisory service rule service is evaluated.<br>Referencing: `/code-invstr-prot-legal-norms/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovr:
          description: "Advisory service override code.<br>Referencing: `/code-overrides/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ovrJustifTempl:
          description: |
            Justification template for the override of the
                      advisory service status.<br>Referencing: `/code-invstr-prot-justifs/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrJustifTemplA:
          description: |
            Auto/Manual flag of Justification template for the override of the
                      advisory service status.
          type: boolean
          readOnly: true
        ovrJustifText:
          description: |
            Justification text for the override of the
                      advisory service status.
          type: string
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        person:
          description: "Advised client for whom this service is offered.<br>Referencing: `/obj-persons/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
          readOnly: true
        replyOvr:
          description: |
            Set to TRUE in order to override the
                      'rule_reply_id'; valid 'ovr_id' required.
          type: boolean
        rule:
          description: "The rule identifier.<br>Referencing: `/code-invstr-prot-rules/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleDiag:
          description: "The rule diagnosis (error or result type).<br>Referencing: `/code-invstr-prot-rule-diags/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ruleMsgKeyOrVal:
          description: |
            The encoded, translatable text (or the
                      non-translatable text itself) for the message explaining the (negative)
                      advisory service reply. Use 'rule_msg' or alternatively
                      bre_text.text#text in order to display the (translated) text.
          type: string
          readOnly: true
        ruleReply:
          description: "The reply of the advisory service rule service.<br>Referencing: `/code-invstr-prot-rule-replys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        status:
          description: "Advisory service status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_inpr_vers:
      title: docp_inpr_vers
      type: object
      properties:
        acusOverStatus:
          description: "Overall asset custom test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        aindOverStatus:
          description: "Overall asset information duty status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        apofOverStatus:
          description: "Overall applicable offering status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        assetList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_asset'
        atstOverStatus:
          description: "Overall appropriateness status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        cobeList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_cobe'
          readOnly: true
        cobeOverStatus:
          description: "Overall cost-benefit test status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        cusOverStatus:
          description: |
            Overall custom test status: the combination of
                      asset ('vers.acus_over_status_id') and service information duty
                      ('inpr.scus_over_status_id') status.<br>Referencing: `/code-invstr-prot-statuss/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        docmOverStatus:
          description: "Overall asset information document status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        induOverStatus:
          description: |
            Overall information duty status: the combination
                      of asset ('vers.aind_over_status_id') and service information duty
                      ('inpr.sind_over_status_id') status.<br>Referencing: `/code-invstr-prot-statuss/{id}`
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        motivTempl:
          description: "Motivation template.<br>Referencing: `/code-invstr-prot-motivs/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        motivText:
          description: "Motivation text for the advisory protocol."
          type: string
          readOnly: true
        overallStatus:
          description: "Overall investor protection status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        partId:
          description: "Part ID"
          type: number
          readOnly: true
        restrList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_restr'
          readOnly: true
        restrOverStatus:
          description: "Overall suitability status.<br>Referencing: `/code-invstr-prot-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        ststList:
          type: array
          items:
            $ref: '#/components/schemas/docp_inpr_stst'
          readOnly: true
        supplmntTempl:
          description: "Supplementary note template.<br>Referencing: `/code-invstr-prot-suppl-notes/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        supplmntText:
          description: "Supplementary note text for the advisory protocol."
          type: string
          readOnly: true
        versionId:
          description: "The ID of the corresponding version of the order."
          type: number
          readOnly: true
    docp_invst_bdl_rule:
      title: docp_invst_bdl_rule
      type: object
      properties:
        agingId:
          description: "ID"
          type: integer
          format: int32
          readOnly: true
        amount:
          description: "Amount to trade"
          type: number
        asset:
          description: "Asset that is the subject of the rule.<br>Referencing: `/obj-assets/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        curry:
          description: "Currency the amount is specified in, if it refers to a currency.<br>Referencing: `/obj-assets/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        estNet:
          description: "Estimated price of the transaction at generation."
          type: number
          readOnly: true
        initEstNet:
          description: "Initial estimated price of the transaction"
          type: number
          readOnly: true
        partId:
          description: "Part ID"
          type: integer
          format: int32
          readOnly: true
        pct:
          description: "Percentage of balance to trade"
          type: number
        qty:
          description: "Quantity to trade"
          type: number
        ruleGrp:
          description: "UI grouping of the rule type.<br>Referencing: `/code-invst-bdl-rule-grps/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ruleId:
          description: "Unique ID to a rule.  Never reused in a bundle."
          type: integer
          format: int32
          readOnly: true
        ruleType:
          description: "Type of Investment Bundler supported rule<br>Referencing: `/code-invst-bdl-rule-types/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
        seqNr:
          description: "Sequence Number"
          type: integer
          format: int32
        status:
          description: "Status of the slave order<br>Referencing: `/code-invst-bdl-rule-statuss/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
          readOnly: true
    docp_recur:
      title: docp_recur
      type: object
      properties:
        asset:
          description: "Recurring order asset<br>Referencing: `/obj-assets/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
        cfCompo:
          $ref: '#/components/schemas/doc_compo_cf'
        deactiv:
          description: "Recurrence deactivated"
          type: boolean
        pos:
          description: "Recurring order position<br>Referencing: `/obj-poss/{id}`"
          allOf:
            - $ref: '#/components/schemas/obj_ref'
    number:
      title: number
      type: object
    obj_key_ref_:
      title: obj_key_ref_
      type: object
      properties:
        objKey:
          description: "Referencing: `/code-obj-keys/{id}`"
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        val:
          description: "Key value"
          type: string
    obj_ref:
      title: obj_ref
      type: object
      properties:
        _href:
          description: "Reference/link"
          type: string
          readOnly: true
        _ident:
          description: "Only used for workItem use cases, cross-reference in workItem context."
          type: string
          writeOnly: true
        id:
          description: "Numeric reference"
          type: integer
          format: int32
        keyList:
          description: "Key value object reference "
          type: array
          items:
            $ref: '#/components/schemas/obj_key_ref_'
          writeOnly: true
    user_ref:
      title: user_ref
      type: object
      properties:
        _href:
          description: "Reference/link"
          type: string
          readOnly: true
        id:
          description: "Numeric reference"
          type: integer
          format: int32
    validationResult:
      title: validationResult
      type: object
      properties:
        displText:
          type: string
          readOnly: true
        ovrHasRight:
          type: boolean
          readOnly: true
        ref:
          description: "Referencing: `/code_ui_errs/{id}` or `/code_overrides/{id}`"
          readOnly: true
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        resType:
          description: "Referencing: `/code_err_types/{id}`"
          readOnly: true
          allOf:
            - $ref: '#/components/schemas/code_tab_ref'
        ovrSignt:
          type: integer
          format: int32

  headers:
    Content-Language:
      schema:
        type: string
    X-Has-Next-Page:
      schema:
        type: string
    Location:
      schema:
        type: string
      description: Location of the created resource
    ETag:
      schema:
        type: string
      description: Current entity-tag

  parameters:
    Filter:
      name: filter
      in: query
      description: Expression to reduce the result set
      schema:
        type: string
    DocPool:
      name: docPool
      style: deepObject
      explode: true
      in: query
      description: "Expression to assign a docPool to reduce the result set <br> Example: <br> docPool[intlId]=stex_client_opn&docPool[objIdList]=22334,55667&docPool[refDateFrom] = 2023-05-01"
      schema:
        type: object
        required: [intlId]
        properties:
          intlId:
            type: string
            example: stex_client_opn
          objIdList:
            type: string
            example: 2222,3333,5555
          refDateFrom:
            type: string
            format: date
          refDateTo:
            type: string
            format: date
    PageLimit:
      name: limit
      in: query
      description: Limits the number of items on a page
      schema:
        type: integer
        default: 5
        minimum: 1
        maximum: 200
    PageOffset:
      name: offset
      in: query
      description: Number of records to be skipped before selecting the result set
      schema:
        type: integer
        minimum: 0
    HrefActive:
      name: href
      in: query
      description: Activates the href
      schema:
        type: boolean
    IncludeInactive:
      name: include_inactive
      in: query
      description: Includes inactive data
      schema:
        type: boolean
    If-None-Match:
      name: If-None-Match
      in: header
      description: Specifies version known by client
      schema:
        type: string
      required: false
    If-Match:
      name: If-Match
      in: header
      description: Specifies entity-tag known by client
      schema:
        type: string
      required: false
    Accept-Encoding:
      name: Accept-Encoding
      in: header
      description: >-
        Compression scheme<br/>
        Warning: HTTP compression might not lead to better performance
      schema:
        type: string
      example: gzip, deflate
      required: false
    Accept-Language:
      name: Accept-Language
      in: header
      description: Specifies the language and locale that the client prefers
      schema:
        type: string
      example: de-CH, en-US
      required: false
    X-Request-ID:
      name: X-Request-ID
      in: header
      description: Specifies the request id in UUID format. If not provided, it will be generated automatically.
      schema:
        type: string
        format: uuid
      required: false
  responses:
    Problem:
      description: RFC7807 Problem
      content:
        application/problem+json:
          schema:
            type: object
            properties:
              type:
                type: string
              title:
                type: string
              status:
                type: number
                format: integer
              detail:
                type: string
              messages:
                type: array
                readOnly: true
                items:
                  type: string
              validationResults:
                type: array
                readOnly: true
                items:
                  $ref: '#/components/schemas/validationResult'
            additionalProperties: true
            required:
              - type
              - title
              - status

