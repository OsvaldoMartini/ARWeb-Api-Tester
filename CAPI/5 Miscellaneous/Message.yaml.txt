openapi: 3.0.2
security:
- bearerAuth: []
info:
  title: Message API
  version: 2.0.2
  x-avq$categories:
  - capi
servers:
- url: /api1/
  description: Development server
paths:
  /msgs:
    get:
      tags:
      - API endpoints
      summary: Get Message references.
      description: |
        Get a collection of `Message` references.
        The result set can be restricted through filter parameter.
        Only `Message` references will be returned that can be accessed by the user (security applied).
        Paging is supported with parameters *limit* and *offset*.
      parameters:
      - $ref: "#/components/parameters/Filter"
      - $ref: "#/components/parameters/PageLimit"
      - $ref: "#/components/parameters/PageOffset"
      - $ref: "#/components/parameters/HrefActive"
      - $ref: "#/components/parameters/IncludeInactive"
      - $ref: "#/components/parameters/Accept-Encoding"
      - $ref: "#/components/parameters/Accept-Language"
      responses:
        "200":
          description: Successful response
          headers:
            Content-Language:
              $ref: "#/components/headers/Content-Language"
            X-Has-Next-Page:
              $ref: "#/components/headers/X-Has-Next-Page"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/msg"
        default:
          $ref: "#/components/responses/Problem"
  /msgs/{msg_id}:
    get:
      tags:
      - API endpoints
      summary: Get a Message reference
      description: |
        *Get a `Message` reference by ID*.
        Only `Message` reference will be returned that can be accessed by the user (security applied).
        If user does not have access to resource, not found error will be returned instead of unauthorized - to disallow attacks.
      parameters:
      - in: path
        name: msg_id
        schema:
          type: string
        required: true
        description: ID of the `Message` reference to get
      - $ref: "#/components/parameters/HrefActive"
      - $ref: "#/components/parameters/IncludeInactive"
      - $ref: "#/components/parameters/Accept-Encoding"
      - $ref: "#/components/parameters/If-None-Match"
      - $ref: "#/components/parameters/Accept-Language"
      responses:
        "200":
          description: Successful response
          headers:
            Content-Language:
              $ref: "#/components/headers/Content-Language"
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/msg"
        "304":
          description: Not modified
        default:
          $ref: "#/components/responses/Problem"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    code_tab_ref:
      title: code_tab_ref
      type: object
      properties:
        _href:
          description: Reference/link
          type: string
          readOnly: true
        id:
          description: Numeric reference
          type: integer
          format: int32
        ident:
          description: Unique symbolic reference
          type: string
        intlId:
          description: Symbolic reference
          type: string
          readOnly: true
    code_tab_ref_action:
      title: code_tab_ref_action
      type: object
      properties:
        _href:
          description: Reference/link
          type: string
          readOnly: true
        action:
          enum:
          - hold
          - proceed
          - discard
          example: hold
          description: enum reference to action
          type: string
          writeOnly: true
        id:
          description: Numeric reference
          type: integer
          format: int32
        ident:
          description: Unique symbolic reference
          type: string
        intlId:
          description: Symbolic reference
          type: string
          readOnly: true
    doc_ref:
      title: doc_ref
      type: object
      properties:
        _href:
          description: Reference/link
          type: string
          readOnly: true
        _ident:
          description: "Only used for workItem use cases, cross-reference in workItem\
            \ context."
          type: string
          writeOnly: true
        id:
          description: Numeric reference
          type: integer
          format: int64
    msg:
      title: msg
      type: object
      properties:
        bu:
          description: "BU ID<br>Referencing: `/obj-bps/{id}`"
          allOf:
          - $ref: "#/components/schemas/obj_ref"
          readOnly: true
        dir:
          description: Direction ("i" | "o" | "io")
          type: string
          readOnly: true
        doc:
          description: "Order<br>Referencing: `/docs/{id}`"
          allOf:
          - $ref: "#/components/schemas/doc_ref"
          readOnly: true
        extlErrCode:
          description: External error code
          type: string
          readOnly: true
        extlRefNr:
          description: External reference number
          type: string
          readOnly: true
        extlStatus:
          description: External message status
          type: string
          readOnly: true
        f1:
          description: |
            Generic field f1. f1 is indexed for fast searches. If you are customizing a standard
            network delivered by Avaloq, we recommend that you do not use these fields or contact the
            business team which provided the network, in order to avoid usage conflicts. Message
            properties should be used instead. It is safe to use this field if you are customizing
            your own network.
          type: string
          readOnly: true
        f10:
          description: |
            Generic field f10. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        f2:
          description: |
            Generic field f2. f2 to f4 are part of a multi-column index that consists of meta_msg_id,
            the timestamp, f2, f3, f4.  If you are customizing a standard network delivered by Avaloq,
            we recommend that you do not use these fields or contact the business team which provided
            the network, in order to avoid usage conflicts. Message properties should be used instead.
            It is safe to use this field if you are customizing your own network.
          type: string
          readOnly: true
        f3:
          description: |
            Generic field f3. f2 to f4 are part of a multi-column index that consists of meta_msg_id,
            the timestamp, f2, f3, f4.  If you are customizing a standard network delivered by Avaloq,
            we recommend that you do not use these fields or contact the business team which provided
            the network, in order to avoid usage conflicts. Message properties should be used instead.
            It is safe to use this field if you are customizing your own network.
          type: string
          readOnly: true
        f4:
          description: |
            Generic field f4. f2 to f4 are part of a multi-column index that consists of meta_msg_id,
            the timestamp, f2, f3, f4.  If you are customizing a standard network delivered by Avaloq,
            we recommend that you do not use these fields or contact the business team which provided
            the network, in order to avoid usage conflicts. Message properties should be used instead.
            It is safe to use this field if you are customizing your own network.
          type: string
          readOnly: true
        f5:
          description: |
            Generic field f5. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        f6:
          description: |
            Generic field f6. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        f7:
          description: |
            Generic field f7. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        f8:
          description: |
            Generic field f8. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        f9:
          description: |
            Generic field f9. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        id:
          description: "Message ID<br>Referencing: `/msgs/{id}`"
          type: integer
          format: int64
          readOnly: true
        metaMsg:
          description: "Message type<br>Referencing: `/meta-msgs/{id}`"
          allOf:
          - $ref: "#/components/schemas/code_tab_ref"
          readOnly: true
        msgStatus:
          description: "Message status<br>Referencing: `/code-msg-statuss/{id}`"
          allOf:
          - $ref: "#/components/schemas/code_tab_ref"
          readOnly: true
        netw:
          description: "Network<br>Referencing: `/code-netws/{id}`"
          allOf:
          - $ref: "#/components/schemas/code_tab_ref"
          readOnly: true
        rcvBp:
          description: "Business Partner<br>Referencing: `/obj-bps/{id}`"
          allOf:
          - $ref: "#/components/schemas/obj_ref"
          readOnly: true
        rcvBpClearNr:
          description: Number
          type: string
          readOnly: true
        sendBp:
          description: "Business Partner<br>Referencing: `/obj-bps/{id}`"
          allOf:
          - $ref: "#/components/schemas/obj_ref"
          readOnly: true
        sendBpClearNr:
          description: Send
          type: string
          readOnly: true
        sendDatePlan:
          description: Send date plan
          type: string
          format: date-time-no-tz
          readOnly: true
        t1:
          description: |
            Generic field f96. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        t2:
          description: |
            Generic field f97. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        t3:
          description: |
            Generic field f98. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        t4:
          description: |
            Generic field f99. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        t5:
          description: |
            Generic field f100. If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        timestamp:
          description: Time when message was inserted
          type: string
          format: date-time-no-tz
          readOnly: true
        x1:
          description: |
            Generic field f11, used for tag 103 in Swift header (Block 3). If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        x2:
          description: |
            Generic field f12, used for Message Prio in Swift header (Block 2). If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        x3:
          description: |
            Generic field f13, used for Validation Flag (tag 119) in Swift header (Block 3). If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        x4:
          description: |
            Generic field f14, used for Banking Prio (tag 113) in Swift header (Block 3). If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
        x5:
          description: |
            Generic field f15, used for UETR (tag 121) in Swift header (Block 3). If you are customizing a standard network delivered by
            Avaloq, we recommend that you do not use these fields or contact the business team which provided the network, in order
            to avoid usage conflicts. Message properties should be used instead. It is safe to use this field if you are
            customizing your own network.
          type: string
          readOnly: true
    msgs:
      title: List of msg
      type: array
      items:
        $ref: "#/components/schemas/msg"
      uniqueItems: true
    obj_key_ref_:
      title: obj_key_ref_
      type: object
      properties:
        objKey:
          description: "Referencing: `/code-obj-keys/{id}`"
          allOf:
          - $ref: "#/components/schemas/code_tab_ref"
        val:
          description: Key value
          type: string
    obj_ref:
      title: obj_ref
      type: object
      properties:
        _href:
          description: Reference/link
          type: string
          readOnly: true
        _ident:
          description: "Only used for workItem use cases, cross-reference in workItem\
            \ context."
          type: string
          writeOnly: true
        id:
          description: Numeric reference
          type: integer
          format: int32
        keyList:
          description: 'Key value object reference '
          type: array
          items:
            $ref: "#/components/schemas/obj_key_ref_"
          writeOnly: true
  headers:
    Content-Language:
      schema:
        type: string
    X-Has-Next-Page:
      schema:
        type: string
    Location:
      schema:
        type: string
      description: Location of the created resource
    ETag:
      schema:
        type: string
      description: Current entity-tag
  parameters:
    Filter:
      name: filter
      in: query
      description: |
        Expression to reduce the result set. Following parameters are mandatory:
        <ul>
          <li><div class="parameter__name required">netw.id|netw.intlId|netw.ident (==)<span>&nbsp;*</span></div></li>
          <li><div class="parameter__name required">timestamp (>=|<=)<span>&nbsp;*</span></div></li>
        </ul>
      schema:
        type: string
      required: true
    PageLimit:
      name: limit
      in: query
      description: Limits the number of items on a page
      schema:
        type: integer
        default: 5
        minimum: 1
        maximum: 200
    PageOffset:
      name: offset
      in: query
      description: Number of records to be skipped before selecting the result set
      schema:
        type: integer
        minimum: 0
    HrefActive:
      name: href
      in: query
      description: Activates the href
      schema:
        type: boolean
    IncludeInactive:
      name: include_inactive
      in: query
      description: Includes inactive data
      schema:
        type: boolean
    If-None-Match:
      name: If-None-Match
      in: header
      description: Specifies version known by client
      schema:
        type: string
      required: false
    If-Match:
      name: If-Match
      in: header
      description: Specifies entity-tag known by client
      schema:
        type: string
      required: false
    Accept-Encoding:
      name: Accept-Encoding
      in: header
      description: "Compression scheme<br/> Warning: HTTP compression might not lead\
        \ to better performance"
      schema:
        type: string
      example: "gzip, deflate"
      required: false
    Accept-Language:
      name: Accept-Language
      in: header
      description: Specifies the language and locale that the client prefers
      schema:
        type: string
      example: "de-CH, en-US"
      required: false
    X-Request-ID:
      name: X-Request-ID
      in: header
      description: "Specifies the request id in UUID format. If not provided, it will\
        \ be generated automatically."
      schema:
        type: string
        format: uuid
      required: false
  responses:
    Problem:
      description: RFC7807 Problem
      content:
        application/problem+json:
          schema:
            type: object
            properties:
              type:
                type: string
              title:
                type: string
              status:
                type: number
                format: integer
              detail:
                type: string
            additionalProperties: true
            required:
            - type
            - title
            - status
