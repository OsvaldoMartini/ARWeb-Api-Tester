{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd $(git rev-parse --show-toplevel) && npx tsc -b packages/domain packages/shared-ui && npx vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/((?!.*\\.).*)", "destination": "/index.html" }
  ]
}
